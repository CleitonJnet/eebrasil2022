<div class="max-w-7xl sm:px-6 lg:px-8 mx-auto">
    <div class="bg-stone-900 bg-opacity-75 overflow-hidden shadow-xl sm:rounded-lg p-2 mt-2 flex justify-end">
        @if (isset($navigation))
            {{ $navigation }}
        @endif
    </div>
</div>
