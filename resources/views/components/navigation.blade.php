<div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
    <div class="bg-gray-700 overflow-hidden shadow-xl sm:rounded-lg p-2 mt-2 flex justify-end">
        @if (isset($navigation))
            {{ $navigation }}
        @endif
    </div>
</div>
